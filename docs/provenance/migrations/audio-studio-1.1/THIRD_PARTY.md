# Third-Party Dependency Record — HRONOAZA Audio Studio 1.1

## Declared dependency

The migrated application declares one development dependency:

- package: `playwright`
- version: `1.62.1`
- scope: `devDependency`
- runtime purpose: headless browser validation only
- production browser application does not import Playwright

## License status

The committed npm lockfile records `playwright 1.62.1` and `playwright-core 1.62.1` with SPDX license identifier `Apache-2.0`. The optional transitive package `fsevents 2.3.2` is recorded as `MIT`.

This record documents the dependency used for repository validation. It does not change or select the license for HRONOAZA Studio itself.

## Reproducibility status

The package version is pinned exactly to `1.62.1`.

`apps/audio-studio/package-lock.json` is now committed from a lockfile generated by the successful GitHub Actions environment.

Verified lockfile SHA-256:

`acd1e7c51430c3815bb5006706aec47af965ca4b81258c0af7696f25f43722a5`

The lockfile freezes the npm dependency graph used for subsequent `npm ci` validation.

## Acceptance implication

The dependency graph is now frozen by the verified lockfile. No automatic project-license choice is made by this record.
